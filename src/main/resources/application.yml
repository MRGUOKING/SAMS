##下面这些内容是为了让MyBatis映射
##指定Mybatis的Mapper文件
#mybatis.mapper-locations=classpath:mappers/*xml
##指定Mybatis的实体目录
#mybatis.type-aliases-package=com.guo.ticket.mybatis.entity

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ticket_manager_system?serverTimezone=GMT%2B8&useSSL=true
    username: root
    password: root
mybatis:
  mapper-locations: classpath:mappers/*xml



